# 我们的爱情纪念网站 💕

一个部署在 Cloudflare Workers 上的浪漫纪念网站，用于记录我们的爱情历程。

## ✨ 特性

- 🌹 浪漫粉色主题，玫瑰元素丰富
- 💫 动态玫瑰花瓣飘落效果
- ⏱️ 实时计算相识/结婚/宝宝天数
- 📖 时间轴展示爱情故事
- 💝 情话轮播（23 条走心文案）
- 📱 完美适配移动端
- ⚡ 快速加载，部署在 Cloudflare 边缘网络

## 🚀 快速开始

### 1. 安装依赖

```bash
npm install
```

### 2. 本地开发

```bash
npm run dev
```

访问 `http://localhost:8787` 查看网站。

### 3. 部署到 Cloudflare Workers

首先确保你已经登录 Cloudflare：

```bash
npx wrangler login
```

然后部署：

```bash
npm run deploy
```

## 📁 项目结构

```
our_love/
├── src/
│   └── worker.ts          # Workers 入口，处理 API 路由
├── public/                # 静态资源
│   ├── index.html         # 首页
│   ├── story.html         # 故事页
│   ├── styles.css         # 样式表
│   ├── main.js            # 首页逻辑
│   ├── story.js           # 故事页逻辑
│   └── roses.js           # 玫瑰动画系统
├── wrangler.toml          # Cloudflare Workers 配置
├── package.json
└── README.md
```

## 🎨 页面说明

### 首页 (`/`)
- 主标题："老婆，我爱你"
- 实时显示相识/结婚/宝宝天数
- 情话轮播（每 5 秒切换）
- 玫瑰花瓣飘落动画
- 点击生成玫瑰粒子

### 故事页 (`/story.html`)
- 时间轴展示三个重要节点：
  - 2020.08.21 相识
  - 2022.01.18 结婚
  - 2024.12.30 宝宝诞生
- 每个节点包含走心文案和图片占位
- 滚动到结尾触发"玫瑰雨"效果

### API (`/api/stats`)
返回 JSON 格式的统计数据：
```json
{
  "met": {
    "days": 1985,
    "hours": 12,
    "minutes": 34,
    "seconds": 56,
    "totalSeconds": 171532496
  },
  "married": {
    "days": 1835,
    "totalDays": 1835
  },
  "baby": {
    "days": 28,
    "totalDays": 28
  },
  "dates": {
    "met": "2020-08-21",
    "married": "2022-01-18",
    "baby": "2024-12-30"
  }
}
```

## ⚙️ 配置修改

### 修改纪念日期

编辑 `src/worker.ts` 和 `public/main.js` 中的日期配置：

```typescript
const DATES = {
  met: '2020-08-21',      // 相识日期
  married: '2022-01-18',  // 结婚日期
  baby: '2024-12-30'      // 宝宝出生日期
};
```

### 修改情话文案

编辑 `public/main.js` 中的 `loveQuotes` 数组，添加或修改你的专属情话。

### 修改故事文案

编辑 `public/story.html` 中的时间轴节点内容。

## 🎯 性能优化

- 移动端自动降低粒子数量（15 个 vs 30 个）
- 支持 `prefers-reduced-motion` 用户偏好
- 使用 Canvas 而非 DOM 元素渲染动画
- 静态资源通过 Cloudflare CDN 分发

## 📱 浏览器兼容性

- Chrome/Edge 90+
- Firefox 88+
- Safari 14+
- 移动端浏览器

## 💡 后续优化建议

1. 替换图片占位为真实照片（放在 `public/` 目录）
2. 添加背景音乐（可选）
3. 增加相册页面
4. 添加留言板功能（需要 KV 存储）

## 📄 许可证

MIT License - 仅供个人使用

---

**用爱构建 ❤️ 部署在 Cloudflare Workers**
